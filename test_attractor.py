from attractor import Attractor
from random import uniform, randint
from math import sqrt
from nose import with_setup

def euler():
    if len(x) == len(y) == len(z) == 10000:
        return True
    else:
        return False